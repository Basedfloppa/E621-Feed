services:
  app:
    image: api:arm64
    ports:
      - "8181:8181"
    volumes:
      - ./config.toml:/app/config.toml:ro
      - ./Rocket.toml:/app/Rocket.toml:ro
    environment:
      - CONFIG_PATH=/app/config.toml
